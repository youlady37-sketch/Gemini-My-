# Server-side Gemini configuration. Never put GEMINI_API_KEY into the extension build.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
PORT="8787"
CORS_ORIGIN=""

# Extension build configuration. Set these only when the deployed backend is available.
VITE_AI_PROVIDER="mock"
VITE_AI_API_URL=""
